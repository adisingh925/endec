<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Endec" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:navigationBarColor">@color/defaultDarkColor</item>
        <item name="android:colorBackground">@color/defaultDarkColor</item>
        <item name="android:statusBarColor">@color/defaultDarkColor</item>
        <item name="icon_color">@color/white</item>
    </style>
</resources>